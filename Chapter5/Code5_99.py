import numpy as np

# Load data from a file 
loaded_data = np.load('data_file.npy')

# Perform calculations (replace this with your specific physics calculations)
mean_value = np.mean(loaded_data)
print(f"Mean value = {mean_value:.2f}")
# Output: Mean fall time = ...
